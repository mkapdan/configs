eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka
    healthcheck:
      enabled: true

spring:
  profiles: dev
  zipkin:
    baseUrl: http://localhost:5411/
    enabled: true