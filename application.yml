eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka
    healthcheck:
      enabled: true

spring:
  profiles: test
  zipkin:
    baseUrl: http://localhost:7411/
    enabled: true