eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka
    healthcheck:
      enabled: true

spring:
  zipkin:
    baseUrl: http://localhost:3411/
    enabled: true