spring:
  profiles: test
server:
  port: 7082
  
turbine:
  clusterNameExpression: new String("default")
  appConfig: ORDERSERVICE  
  combineHostPort: true
---
spring:
  profiles: docker
server:
  port: 6082